<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.education.mapper.system.SystemDictMapper">

   <select id="queryList" resultType="map">
       select * from system_dict a <include refid="condition"/>
   </select>

    <select id="listGroup" resultType="modelBeanMap">
         select a.*, b.name as parent_name from system_dict a left join system_dict b on a.parent_id = b.id
         <include refid="condition"/>
         group by type
    </select>

    <sql id="condition">
        <where>
            <if test="type != null and type != ''">
                and a.type = #{type}
            </if>

            <if test="code != null and code != ''">
                and a.code = ${code}
            </if>

            <if test="parent_id != null and parent_id != ''">
                and a.parent_id = ${parent_id}
            </if>
        </where>
        order by a.sort
    </sql>
</mapper>